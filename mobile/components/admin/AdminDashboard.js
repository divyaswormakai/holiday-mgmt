import React from 'react';
import { Text, View } from 'react-native';

import { vw } from '../../utils/viewport';

const AdminDashboard = () => {
	return (
		<View>
			<Text style={{ fontSize: 6 * vw }}>Admin Details</Text>
		</View>
	);
};

export default AdminDashboard;
