import React from 'react';
import { Text, View } from 'react-native';

const AdminRequestFormModal = ({ requestDetails, setShowModal }) => {
	return (
		<View>
			<Text>This will be where we will be editing the request forms</Text>
		</View>
	);
};

export default AdminRequestFormModal;
