import React from 'react';
import { Text, View } from 'react-native';

const AdminSupportTicketFormModal = ({ ticketDetails, setShowModal }) => {
	return (
		<View>
			<Text>This will be where we will be editing the tickets forms</Text>
		</View>
	);
};

export default AdminRequestFormModal;
