import React from "react";
import { Text, View } from "react-native";

const UserDashboard = ({ profileDetails }) => {
	return (
		<View>
			<Text>Status Report</Text>
			<Text>Cards to show shit</Text>
		</View>
	);
};

export default UserDashboard;
