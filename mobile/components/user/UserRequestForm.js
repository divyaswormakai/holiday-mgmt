import React from "react";
import { Text, View } from "react-native";

const UserRequestForm = () => {
	return (
		<View>
			<Text>This is the request form</Text>
		</View>
	);
};

export default UserRequestForm;
